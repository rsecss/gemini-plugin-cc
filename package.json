{
  "name": "@community/gemini-plugin-cc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Use Gemini from Claude Code to review code or delegate tasks.",
  "license": "MIT",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:ci": "node --test --test-reporter=spec tests/*.test.mjs"
  }
}
